#!/usr/bin/node

// prints 3 lines with a loop.

let i = 0;
const printlines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

while (i < printlines.length) {
  console.log(printlines[i]);
  i++;
}
